# Tmux install & setup

## Install

Run `install.sh`. This will download all deps and install tmux with autocompletion and set symlink from `.tmux.conf` to repo `.tmux.conf` so to be in sync with remote config
